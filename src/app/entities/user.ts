export interface User {
    firstName: string
    lastName: string
    nickName: string
    productsBuyed: string[]
    id: string
  }

export interface UserInfo{
    firstName: string
    lastName: string   
}